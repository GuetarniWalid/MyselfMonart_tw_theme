{% comment %}
    Renders buy buttons for the product.

    Accepts:
    - id: {String} A unique ID
    - toWrap: {Boolean} If to add lottie-wrapper or not
    - src: {String} An url to a lootie file
    - style: {String} A string that represents css
    - mode: {String} The mode of the animation
    - actions: {String} The actions of the animation
    - loop: {Boolean} If a loop is active on the animation
    - autoplay: {Boolean} If the animation should autoplay
    - speed: {String} The speed of the animation

    Usage:
    {% render 'component-lottie', id: {String}, src: {String}, style: {String}, mode: {String}, actions: {String}, loop: {Boolean}, autoplay: {Boolean}, speed: {String} %}
{% endcomment %}

{%- if toWrap -%}
    <lottie-wrapper data-shadow-style="{{ style }}" data-actions="{{ actions }}" data-mode="{{ mode }}">
{%- endif -%}
        <lottie-player id="{{ id }}" src="{{ src }}"  background="transparent" {% if loop %}loop{% endif %} {% if autoplay %}autoplay{% endif %} {% if speed %}speed="{{ speed }}"{% endif %}>
        </lottie-player>
{%- if toWrap -%}
    </lottie-wrapper>
{%- endif -%}