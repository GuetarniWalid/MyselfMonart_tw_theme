<section class="home-section home-usps">
  <div class="home-container">
    {%- if section.settings.eyebrow != blank or section.settings.title != blank or section.settings.intro != blank -%}
      <div class="home-usps__header">
        {%- if section.settings.eyebrow != blank -%}
          <p class="home-eyebrow">{{ section.settings.eyebrow | escape }}</p>
        {%- endif -%}
        {%- if section.settings.title != blank -%}
          <h2 class="home-h2">{{ section.settings.title | escape }}</h2>
        {%- endif -%}
        {%- if section.settings.intro != blank -%}
          <p class="home-lead">{{ section.settings.intro }}</p>
        {%- endif -%}
      </div>
    {%- endif -%}

    <div class="home-usps__grid">
      {%- for block in section.blocks -%}
        <article class="home-usp" {{ block.shopify_attributes }}>
          {%- if block.settings.number != blank -%}
            <div class="home-usp__number">{{ block.settings.number | escape }}</div>
          {%- endif -%}
          {%- if block.settings.title != blank -%}
            <h3 class="home-usp__title">{{ block.settings.title | escape }}</h3>
          {%- endif -%}
          {%- if block.settings.text != blank -%}
            <div class="home-usp__text">{{ block.settings.text }}</div>
          {%- endif -%}
        </article>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Home — USPs",
  "tag": "section",
  "enabled_on": { "templates": ["index"] },
  "max_blocks": 6,
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow" },
    { "type": "text", "id": "title", "label": "Titre H2" },
    { "type": "textarea", "id": "intro", "label": "Intro" }
  ],
  "blocks": [
    {
      "type": "usp",
      "name": "USP",
      "settings": [
        { "type": "text", "id": "number", "label": "Chiffre (ex: 01)", "default": "01" },
        { "type": "text", "id": "title", "label": "Titre H3" },
        { "type": "richtext", "id": "text", "label": "Texte" }
      ]
    }
  ],
  "presets": [
    { "name": "Home — USPs", "blocks": [{ "type": "usp" }, { "type": "usp" }, { "type": "usp" }, { "type": "usp" }] }
  ]
}
{% endschema %}
